import { createResult } from './lib/create-result';

declare global {
  namespace jest {
    interface Expect {
      /**
       * <%- description %>.
       * @example
       * <%- asymmetricExample %>
       */
      <%- camelName %><T>(<%- argumentsList.join(', ') %>): JestMatchers<T>;
    }
    interface Matchers<R, T> {
      /**
       * <%- description %>.
       * @example
       * <%- example %>
       */
      <%- camelName %>(<%- argumentsList.join(', ') %>): R;
    }
  }
}

export const <%- camelName %>Matcher = (<%- ['received: any'].concat(argumentsList).join(', ') %>) =>
  createResult({
    message: () => `<%- failMessage %>`,
    notMessage: () => `<%- notFailMessage %>`,
    pass: received === '@TODO: replace this with implementation'
  });

expect.extend({ <%- camelName %>: <%- camelName %>Matcher });
